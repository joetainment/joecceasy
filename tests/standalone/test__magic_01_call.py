#!/usr/bin/env python3
from joecceasy import Easy; exec( Easy.Magic ); exit() #%exit  
## the above line needs the magic, at its end: #%exit
  
## Easy.Magic is the code of the running script,
## (automatically after doing initializations
## and automatically translated
## so that lines with magic tokens,
## write out automated python source code,
## which is executed.

## The call below will run the system command "dir"
#%call dir

## result would is available in local var: tmpReturned
# print( tmpReturned.stdout )
